# Fixture: stage-3 engine on a grouped cluster (group1/cluster2). Exercises
# the loader's group-level hierarchy walk plus .yaml.gotmpl variants at
# global, group, and cluster levels — the values referenced by the .gotmpl
# files chain through earlier-loaded entries (progressive accumulator).
apps:
  - template: app-stage3-probe
    namespace: test
settings:
  branch: main
  autoSync: false
