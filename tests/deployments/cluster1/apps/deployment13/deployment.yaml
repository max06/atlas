# Fixture: multi-line string values across all three hierarchy source formats
# (plain yaml, gotmpl, sops) round-trip through the store trace and the
# rendered output intact — no document corruption, no truncation, no escaping.
apps:
  - template: app-novals
    namespace: test
settings:
  branch: main
  autoSync: false
