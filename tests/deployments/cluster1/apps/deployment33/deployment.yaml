# Fixture: stage-3 named instances. Same template instantiated twice with
# distinct apps[].name values; each must render its own release with
# atlas.instance.name correctly scoped.
apps:
  - template: app-stage3-named
    name: primary
    namespace: test
  - template: app-stage3-named
    name: secondary
    namespace: test
settings:
  branch: main
  autoSync: false
