# Fixture: named instances. Same template instantiated twice with
# distinct apps[].name values; each must render its own release with
# atlas.instance.name correctly scoped.
apps:
  - template: app-named-multi
    name: primary
    namespace: test
  - template: app-named-multi
    name: secondary
    namespace: test
settings:
  branch: main
  autoSync: false
