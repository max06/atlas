# Fixture: stage-3, condition + installed stripping.
# See tests/templates/app-stage3-condinst/helmfile.yaml.gotmpl for context.
apps:
  - template: app-stage3-condinst
    namespace: test
settings:
  branch: main
  autoSync: false
