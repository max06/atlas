# Fixture: condition + installed stripping.
# See tests/templates/app-condinst/helmfile.yaml.gotmpl for context.
apps:
  - template: app-condinst
    namespace: test
settings:
  branch: main
  autoSync: false
