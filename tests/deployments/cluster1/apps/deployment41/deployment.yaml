# Fixture: template-render-time access to hierarchy values
# (jsonPatches inline + inline values map). See app-tplhier.
apps:
  - template: app-tplhier
    namespace: test
settings:
  branch: main
  autoSync: false
