# Fixture: stage-3 template-render-time access to hierarchy values
# (jsonPatches inline + inline values map). See app-stage3-tplhier.
apps:
  - template: app-stage3-tplhier
    namespace: test
settings:
  branch: main
  autoSync: false
