# Fixture: release-context access. Exercises the values-loader where
# .Release.* is available at evaluation time alongside the full atlas object.
apps:
  - template: app-stage3-probe
    namespace: test
settings:
  branch: main
  autoSync: false
