# Fixture: top-level template fields (repositories, helmDefaults, ...)
# pass through ATLAS verbatim to the emitted helmfile state.
apps:
  - template: app-stage3-passthrough
    namespace: test
settings:
  branch: main
  autoSync: false
