# Fixture: hierarchy .yaml.gotmpl that references .Values.atlas.*
# at hierarchy-walk time. See sibling values.yaml.gotmpl.
apps:
  - template: app-hier-atlas-ref
    namespace: test
settings:
  branch: main
  autoSync: false
